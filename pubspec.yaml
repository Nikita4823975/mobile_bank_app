name: mobile_bank_app
description: "A new Flutter project."

version: 1.0.0+1

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  mysql1: ^0.20.0   # изменено с 0.22.0 на 0.20.0
  http: ^1.4.0       # изменено с 0.15.0 на 1.4.0
  shared_preferences: ^2.2.2
  flutter_secure_storage: ^9.2.4
  intl: ^0.18.1
  provider: ^6.1.5
  jwt_decoder: ^2.0.1
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
